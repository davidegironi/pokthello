{
  "name": "pokthello",
  "description": "A Pokemon to Shakespeare description translator",
  "author": "Davide Gironi",
  "license": "GPLv3",
  "scripts": {
    "dockerbuild": "docker-compose build",
    "herokuship": "cd pokthello-web && npm run herokubuild && cd .. && git commit -am 'heroku push' && git push heroku `git subtree split --prefix pokthello-api master`:master --force"
  }
}